language: java
jdk:
  # current LTS versions
#  - openjdk8    # Disabled because moditect needs JDK 9+ (though we build in JDK 7 mode) 
  # already EOL
#  - openjdk9
  # feature release
  - openjdk10
  # upcoming LTS
  - openjdk11
  # early access
  - openjdk-ea
  # current LTS versions
#  - oraclejdk8    # As above
  # already EOL
#  - oraclejdk9
  # feature release
  - oraclejdk10
  # upcoming LTS
#  - oraclejdk11
  # early access
#  - oraclejdk-ea
