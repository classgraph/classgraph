package io.github.classgraph.features.externalpackage.accepted;

import io.github.classgraph.features.externalpackage.external.ExternalSuperclass;

/** A class in the accepted package, whose superclass is in a package that is not accepted. */
public class AcceptedSubclass extends ExternalSuperclass {
}
