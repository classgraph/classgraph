package io.github.classgraph.features.globs;

/** A glob test fixture, directly in the {@code globs} package. */
public class ShallowGlobSuffix {
    /** An inner class, so that a glob can be tested against a {@code '$'}. */
    public static class Inner {
    }
}
